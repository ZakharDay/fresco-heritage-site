const speed = 100
const paintingSize = 100
const grid = { columns: 6, rows: 8 }
const size = 9

const types = [
  { columns: 2, rows: 2 },
  { columns: 2, rows: 3 },
  { columns: 3, rows: 2 },
  { columns: 3, rows: 4 },
  { columns: 4, rows: 3 }
]

export { speed, paintingSize, grid, types }
